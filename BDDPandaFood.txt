DROP TABLE usuario cascade;
DROP TABLE factura cascade;
DROP TABLE cliente cascade;


CREATE TABLE usuario (nombre_usuario varchar(20) not null PRIMARY KEY, contraseña varchar(20) not null);

CREATE TABLE factura (codigo_factura int not null PRIMARY KEY, fecha date not null,precio_total int not null,cedula_cliente int not null);

CREATE TABLE cliente (cedula int not null PRIMARY KEY, nombre varchar(20) not null, apellido varchar(20) not null,puntos int not null,codigo_factura int not null, FOREIGN KEY (codigo_factura) REFERENCES factura (codigo_factura) );

ALTER TABLE factura ADD CONSTRAINT FKtest FOREIGN KEY (cedula_cliente) REFERENCES cliente (cedula);